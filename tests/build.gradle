if (!hasProperty('mainClass')) {
    ext.mainClass = ''
}

dependencies {
    // Implementation as we also expose the lwjgl classes as main dependency.
    implementation 'org.junit.jupiter:junit-jupiter-api:5.4.2'
    implementation project(':harness')

    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.4.2'
    implementation 'org.jmonkeyengine:jme3-core'
    testRuntimeOnly 'org.jmonkeyengine:jme3-desktop'
    testRuntimeOnly 'org.jmonkeyengine:jme3-lwjgl'
}

test {
    useJUnitPlatform()
}